# Arkheionx Semantic-Lite Fixture

This fixture demonstrates false-positive reduction.

The docs intentionally mention oracle, rewards, reentrancy, and vault concepts
as planning vocabulary. Those terms alone should not create high-confidence
findings. The Solidity source includes one real oracle call so Arkheionx can
attach function-level semantic-lite evidence.

This is toy code only. It is not production code and must not be deployed.

