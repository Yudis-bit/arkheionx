"""Arkheionx protocol-map engine (additive workbench layer).

Import :func:`arkheionx.protocol.detector.analyze` directly to avoid eager
import cycles between the protocol, flow, and hunt packages.
"""
