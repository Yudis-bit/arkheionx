"""Bundled, local-only Arkheionx demo fixtures (package data).

These are toy fixtures shipped as package resources so `arkheionx demo --copy`
works from an installed package, not only from a source checkout. They contain
no secrets, RPC URLs, private keys, or live-chain instructions.
"""
