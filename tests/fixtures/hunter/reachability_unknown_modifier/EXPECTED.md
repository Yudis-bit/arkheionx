# EXPECTED — C: unknown custom modifier

- `UnknownGate.setValue` / `UnknownGate.withdraw` -> `UNKNOWN_MODIFIER_GATED_EXTERNAL` (UNKNOWN).
- NOT `UNPRIVILEGED_EXTERNAL` (conservative: who-can-call is unresolved).
- Decision cap: PARK_REACHABILITY; Submit:NO; no PoC plan unless a modifier-bypass hypothesis exists.
