# Scope — Inline Assembly Role Getter

## In scope
- `AssemblyOracleRate` updateRate is oracle-gated via an assembly sload getter.

## Reward / severity
- Valid severity: Critical, High, Medium. Medium and above is eligible for a reward.
- PoC required for High and Critical.
