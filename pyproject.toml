[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "arkheionx"
version = "2.10.0.dev0"
description = "Foundry-style local security workbench for DeFi protocol mapping, money-flow analysis, hunter ranking, and execution-aware proof artifacts."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Yudistira Putra" }
]
keywords = [
  "defi-security",
  "smart-contract-security",
  "value-flow",
  "security-testing",
  "audit-readiness",
  "test-coverage",
  "sarif",
  "foundry",
  "solidity"
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Security",
  "Topic :: Software Development :: Quality Assurance"
]
dependencies = []

[project.urls]
Homepage = "https://github.com/Yudis-bit/DeFi-Exploit-PoCs"
Documentation = "https://github.com/Yudis-bit/DeFi-Exploit-PoCs#readme"

[project.scripts]
arkheionx = "arkheionx.cli.main:main"

[tool.setuptools.packages.find]
include = ["arkheionx*"]

[tool.setuptools.package-data]
"arkheionx.demo.fixtures" = ["**/*.md", "**/*.toml", "**/*.sol"]
