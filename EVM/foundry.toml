[profile.default]
solc = "0.8.20"
src = "src"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 200
via_ir = true

remappings = ['@src/=src/', '@lib/=lib/']

[rpc_endpoints]
mainnet = "${ETH_RPC_URL}"
