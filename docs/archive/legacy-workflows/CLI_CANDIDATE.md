# Archived CLI CANDIDATE

This archived CLI document is preserved for historical context.

It is not the current public CLI reference.

Current CLI reference: [CLI_REFERENCE.md](../../CLI_REFERENCE.md).

This file remains in the repository so older references and release-history checks can resolve without presenting legacy CLI planning notes as the current public entry point.
