# Archived CLI COMMANDS

This archived CLI document is preserved for historical context.

It is not the current public CLI reference.

Current CLI reference: [CLI_REFERENCE.md](../../CLI_REFERENCE.md).

## Historical command coverage

This legacy CLI planning document previously covered older command-surface notes, including:

- `validate-config`
- `test-plan`

The command reference has since moved to the canonical public CLI reference, but this archived file remains in the repository so older references and release-history checks can resolve without presenting legacy CLI planning notes as the current public entry point.
