<!-- BEGIN GENERATED VERIFICATION SUMMARY -->

# Verification Report — 2020-06-balancer-deflationary

- **Entry id:** `2020-06-balancer-deflationary`
- **Title:** Bancor — public safeTransferFrom on newly deployed contract
- **Protocol:** Bancor
- **Date of incident:** 2020-06
- **VM:** EVM
- **Chain:** ethereum
- **Fork block:** `10307563`
- **RPC alias:** `mainnet`
- **Category:** `access-control-failure`
- **Reproducibility:** `deterministic-likely-but-unverified`
- **Verification status:** `not-run-no-rpc`

## Command to run

```sh
cd EVM && forge test --match-path "test/2020-06/Exploit_2020-06.t.sol" -vvv
```

## Required assertion families

- **F4** (Unauthorized state transition assertion) or **F7** (Ownership / control assertion)

Family definitions are in [`docs/ASSERTION_STANDARD.md`](../../docs/ASSERTION_STANDARD.md).

## Current verification status

**Not verified locally because required archival RPC was not configured at report-generation time.**

The metadata records `reproducibility: deterministic-likely-but-unverified` and `verification_status: not-run-no-rpc`. To produce a verified run, configure the appropriate `*_RPC_URL` env var (see [`docs/FORK_VERIFICATION.md`](../../docs/FORK_VERIFICATION.md)), execute the command above, and replace this section with the run transcript.

## Static Assertion Review (Phase 5)

- **Assertion quality (static):** `strong`
- **Attacker profit check (intended):** Attacker XBPToken balance after attack > pre-attack balance by the victim's drained amount.
- **Victim loss check (intended):** Victim XBPToken balance after attack equals zero (or pre-attack minus drained amount).
- **Archival RPC required for final proof:** yes

**Static review notes (from `metadata.notes`):**

> Phase 6A reclassification: PoC code is the Bancor public-safeTransferFrom incident (June 2020), not the Balancer STA deflationary-token drain. Evidence: contract Exploit__202006 forks block 10307563 and calls IBancor(0x5f58058C0eC971492166763c8C22632B583F667f).safeTransferFrom(XBPToken=0x28dee0...,victim=0xfd0B4D...,attacker,victim_balance); source comment header reads 'Bancor Protocol Access Control Exploit PoC'. id slug retained to keep file paths stable; aggregate loss_usd intentionally absent pending manual confirmation. Protocol identity is high-confidence; status: needs-verification reflects archival fork run still pending. Phase 6E: additive assertions added in EVM/test/2020-06/Exploit_2020-06.t.sol (allowanceBefore >= victimBefore precondition; victimAfter == 0 and attackerAfter - attackerBefore == victimBefore postconditions). Patch compiles and forge fmt-clean; assertion logic unexecuted because public RPC returned 'historical state is not available' for block 10307563 — archival mainnet RPC required for execution. assertion_quality raised none→strong on static grounds; verification_status retained as not-run-no-rpc.

## Attacker path

Identify a user who has approved the Bancor contract for an ERC-20, then call its public safeTransferFrom to move that user's tokens to the attacker.

## Invariant broken

Token approvals to a contract are spendable only by the contract's intended internal flows, not by arbitrary callers.

## Victim impact

Several users with active token approvals to the Bancor contract were drained; aggregate loss not authoritatively confirmed.

**Loss check:** Victim XBPToken balance after attack equals zero (or pre-attack minus drained amount).

## Root cause

Internal helper safeTransferFrom was left with public visibility on a deployed contract that already held outstanding user approvals.

**Protocol assumption that failed:** Assumed the helper that consumes user approvals was internal; deployment exposed it as public.

## References

- [Example attack tx (cited in PoC source comment)](https://etherscan.io/tx/0x4643b63dcbfc385b8ab8c86cbc46da18c2e43d277de3e5bc3b4516d3c0fdeb9f)

## Missing verification steps

- [ ] Configure archival RPC for chain `ethereum` (alias `mainnet`).
- [ ] Run the command above against the declared fork block.
- [ ] Confirm all required assertion families pass.
- [ ] Replace the current-status section with run output.
- [ ] Update metadata `reproducibility` to `deterministic-confirmed` and `verification_status` to `verified` in the same change.

## Verifier

- **Handle:** _to be filled by verifier_
- **Date:** _to be filled by verifier_
- **Repository commit:** _to be filled by verifier_

---

Generated by `scripts/generate_verification_report.py` from `metadata/registry.json`. Content between the BEGIN/END GENERATED VERIFICATION SUMMARY markers is regenerated on every run. Manual sections (run transcripts, smoke-test evidence, archival verification logs) must live OUTSIDE those markers and will be preserved.

<!-- END GENERATED VERIFICATION SUMMARY -->

## Planned Assertion Hardening (Phase 6B)

- **Proposed assertions:**
  - Snapshot `victimBefore = XBPToken.balanceOf(victim)`, `attackerBefore = XBPToken.balanceOf(attacker)`, and `allowanceBefore = XBPToken.allowance(victim, bancorAddress)` before the existing prank.
  - `assertGe(allowanceBefore, victimBefore, ...)` as the F4 precondition guard.
  - After `bancorContract.safeTransferFrom(...)`: `assertEq(attackerAfter - attackerBefore, victimBefore, ...)` and `assertEq(victimAfter, 0, ...)`.
- **Proof target:** F4 (unauthorized state transition — the public helper consumed an approval the caller had no business spending) and F1 (value moved — exact-equality drain).
- **Risk:** additive-only. Three pre-snapshots, two post-snapshots, three assertions; transfer amount changes from inline `XBPToken.balanceOf(victim)` to the snapshotted `victimBefore` (mathematically identical, no behavior change).
- **Requires archival RPC:** yes — block 10307563 is archival-only on mainnet.
- **Patch class:** additive-only but requires archival verification.

## Phase 6A Reclassification (manual, persistent)

- **Previous label:** Balancer — deflationary token rounding (`fee-on-transfer-rebasing-assumption`)
- **Code-observed label:** Bancor — public `safeTransferFrom` on newly deployed contract (`access-control-failure`)
- **Decision:** Reclassify metadata to match code; retain `id` slug `2020-06-balancer-deflationary` to preserve file paths and prior cross-references; status set to `needs-verification`.
- **Evidence from code (`EVM/test/2020-06/Exploit_2020-06.t.sol`):**
  - Source comment header: "Bancor Protocol Access Control Exploit PoC".
  - `bancorAddress = 0x5f58058C0eC971492166763c8C22632B583F667f`.
  - `victim = 0xfd0B4DAa7bA535741E6B5Ba28Cba24F9a816E67E`, `XBPToken = 0x28dee01D53FED0Edf5f6E310BF8Ef9311513Ae40`.
  - Fork block `10307563`; only call against the cited contract is `bancorContract.safeTransferFrom(XBPToken, victim, attacker, victim_balance)`.
  - PoC source links example tx `0x4643b63d...0fdeb9f`.
- **Remaining uncertainty:** aggregate USD loss from the wider Bancor incident not authoritatively confirmed from PoC alone; only the single-victim drain is exercised here.
- **Verification status:** unchanged — `not-run-no-rpc`.

## Phase 6E Assertion Patch

- **Identity reminder:** registry id and file path remain `2020-06-balancer-deflationary` for stability; the code-observed protocol is **Bancor** (public `safeTransferFrom`), per Phase 6A reclassification.
- **File modified:** `EVM/test/2020-06/Exploit_2020-06.t.sol` (test only; `EVM/src/**` untouched).
- **Assertions added (additive-only):**
  - Pre-state snapshots: `victimBefore = XBPToken.balanceOf(victim)`, `attackerBefore = XBPToken.balanceOf(attacker)`, `allowanceBefore = XBPToken.allowance(victim, bancorAddress)`.
  - F4 precondition guard: `assertGe(allowanceBefore, victimBefore, ...)` — confirms the victim's outstanding ERC-20 approval to the Bancor contract is large enough to cover the drained amount, the necessary precondition for the public-helper abuse.
  - Post-state snapshots: `victimAfter`, `attackerAfter` after the existing `safeTransferFrom` call.
  - F4 / F1 post-conditions:
    - `assertEq(victimAfter, 0, ...)` — full drain to zero.
    - `assertEq(attackerAfter - attackerBefore, victimBefore, ...)` — attacker delta equals the victim's pre-attack balance, exact equality.
- **Proof target:** F4 (unauthorized state transition — public helper consumed an allowance the caller had no business spending) and F1 (value-moved exact-equality drain).
- **Behavioral change:** none. Transfer amount switched from inline `XBPToken.balanceOf(victim)` to the snapshotted `victimBefore`; mathematically identical at the same block. No change to addresses, call order, prank, or attacker actions.
- **Command run:**

  ```sh
  cd EVM && forge test --match-path test/2020-06/Exploit_2020-06.t.sol -vvv
  ```

- **Result:** `setUp()` failed with
  `vm.createSelectFork: ... historical state ... is not available` (public mainnet RPC returned `error code -32000`).
- **Did the assertions execute?** No — the test never reached `testsafeTransfer()` because the fork could not be created. This is an **archival-RPC limitation**, not a PoC defect.
- **Static correctness:** patch compiles cleanly under `forge build` and passes `forge fmt --check`. Assertion logic is additive-only and matches the documented invariant.
- **Remaining verification limitation:** archival mainnet RPC required for block 10307563; without it, the assertions cannot be exercised. Verification status therefore stays `not-run-no-rpc` and reproducibility stays `deterministic-likely-but-unverified`. Do not promote to `verified` / `deterministic-confirmed` from this run.

