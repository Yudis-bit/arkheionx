<!-- BEGIN GENERATED VERIFICATION SUMMARY -->

# Verification Report — 2017-11-parity-suicide

- **Entry id:** `2017-11-parity-suicide`
- **Title:** Parity Wallet Library — suicide
- **Protocol:** Parity Multisig Library
- **Date of incident:** 2017-11
- **VM:** EVM
- **Chain:** ethereum
- **Fork block:** `4501735`
- **RPC alias:** `mainnet`
- **Category:** `access-control-failure`
- **Reproducibility:** `deterministic-likely-but-unverified`
- **Verification status:** `not-run-no-rpc`

## Command to run

```sh
cd EVM && forge test --match-path "test/2017-11/Exploit_2017-11.t.sol" -vvv
```

## Required assertion families

- **F4** (Unauthorized state transition assertion) or **F7** (Ownership / control assertion)

Family definitions are in [`docs/ASSERTION_STANDARD.md`](../../docs/ASSERTION_STANDARD.md).

## Current verification status

**Not verified locally because required archival RPC was not configured at report-generation time.**

The metadata records `reproducibility: deterministic-likely-but-unverified` and `verification_status: not-run-no-rpc`. To produce a verified run, configure the appropriate `*_RPC_URL` env var (see [`docs/FORK_VERIFICATION.md`](../../docs/FORK_VERIFICATION.md)), execute the command above, and replace this section with the run transcript.

## Static Assertion Review (Phase 5)

- **Assertion quality (static):** `weak`
- **Attacker profit check (intended):** Library is selfdestructed; downstream wallet calls revert. (No direct attacker profit.)
- **Victim loss check (intended):** Library code at the well-known address is empty post-suicide; dependent wallets cannot delegatecall.
- **Archival RPC required for final proof:** yes

**Static review notes (from `metadata.notes`):**

> Phase 5 static review: only the ownership hijack is asserted (assertTrue isowner). No assertion that the library actually self-destructs. Recommended Phase 6 patch: capture address(WalletLibrary).code.length pre and post and assert it transitions from non-zero to zero (valid for the pre-Cancun fork block).

## Attacker path

Initialize the shared library to take ownership, then call its kill function.

## Invariant broken

A shared library cannot be self-destructed by an unrelated caller.

## Victim impact

513,774.16 ETH frozen across 587 wallets.

**Loss check:** Library code at the well-known address is empty post-suicide; dependent wallets cannot delegatecall.

## Root cause

Library contract had unprotected initialization and a privileged kill function reachable through delegatecall semantics.

**Protocol assumption that failed:** Assumed the library was permanently anchored by its consumers; nothing prevented direct ownership.

## References

- [Parity post-mortem](https://www.parity.io/a-postmortem-on-the-parity-multi-sig-library-self-destruct/)

## Missing verification steps

- [ ] Configure archival RPC for chain `ethereum` (alias `mainnet`).
- [ ] Run the command above against the declared fork block.
- [ ] Confirm all required assertion families pass.
- [ ] Replace the current-status section with run output.
- [ ] Update metadata `reproducibility` to `deterministic-confirmed` and `verification_status` to `verified` in the same change.

## Verifier

- **Handle:** _to be filled by verifier_
- **Date:** _to be filled by verifier_
- **Repository commit:** _to be filled by verifier_

---

Generated by `scripts/generate_verification_report.py` from `metadata/registry.json`. Content between the BEGIN/END GENERATED VERIFICATION SUMMARY markers is regenerated on every run. Manual sections (run transcripts, smoke-test evidence, archival verification logs) must live OUTSIDE those markers and will be preserved.

<!-- END GENERATED VERIFICATION SUMMARY -->

## Planned Assertion Hardening (Phase 6B)

- **Proposed assertions:**
  - `assertGt(address(WalletLibrary).code.length, 0, ...)` immediately before `WalletLibrary.kill(...)`.
  - `assertEq(address(WalletLibrary).code.length, 0, ...)` immediately after `WalletLibrary.kill(...)`.
- **Proof target:** F4 (unauthorized state transition) and F7 (ownership/control). The destructive `SELFDESTRUCT` is observed by transition of the library bytecode from non-zero to zero in the same transaction (valid pre-Cancun; fork block 4501735 is well before Cancun activation).
- **Risk:** additive-only and low risk. Two pure `code.length` reads plus one `assertEq`; no extra prank, no extra external call to the exploit target, no transfer-amount change.
- **Requires archival RPC:** yes — block 4501735 is archival-only on mainnet.
- **Patch class:** additive-only and low risk. Recommended Phase 6C target.