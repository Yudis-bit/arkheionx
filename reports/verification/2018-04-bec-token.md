<!-- BEGIN GENERATED VERIFICATION SUMMARY -->

# Verification Report — 2018-04-bec-token

- **Entry id:** `2018-04-bec-token`
- **Title:** BeautyChain (BEC) — batchTransfer overflow
- **Protocol:** BeautyChain Token (BEC)
- **Date of incident:** 2018-04
- **VM:** EVM
- **Chain:** ethereum
- **Fork block:** `5483642`
- **RPC alias:** `mainnet`
- **Category:** `arithmetic-precision-rounding`
- **Reproducibility:** `deterministic-likely-but-unverified`
- **Verification status:** `not-run-no-rpc`

## Command to run

```sh
cd EVM && forge test --match-path "test/2018-04/Exploit_2018-04.t.sol" -vvv
```

## Required assertion families

- **F1** (Attacker profit assertion) or **F3** (Invariant break assertion)

Family definitions are in [`docs/ASSERTION_STANDARD.md`](../../docs/ASSERTION_STANDARD.md).

## Current verification status

**Not verified locally because required archival RPC was not configured at report-generation time.**

The metadata records `reproducibility: deterministic-likely-but-unverified` and `verification_status: not-run-no-rpc`. To produce a verified run, configure the appropriate `*_RPC_URL` env var (see [`docs/FORK_VERIFICATION.md`](../../docs/FORK_VERIFICATION.md)), execute the command above, and replace this section with the run transcript.

## Static Assertion Review (Phase 5)

- **Assertion quality (static):** `strong`
- **Attacker profit check (intended):** Attacker BEC balance after attack approximates the overflowed supply.
- **Victim loss check (intended):** Token totalSupply no longer matches sum of legitimate holder balances.
- **Archival RPC required for final proof:** yes

**Static review notes (from `metadata.notes`):**

> Phase 6D: additive assertions added — assertEq(a1After-a1Before, type(uint256).max/2+1), assertEq(a2After-a2Before, type(uint256).max/2+1), and assertGt(a1After+a2After, totalSupply()) capturing the canonical batchTransfer overflow fingerprint (count*value wraparound + invariant break). Static proof target is now exact; runtime verification still pending archival mainnet RPC at block 5483642 (public RPC returned 'historical state ... is not available' on 2026-05-22).

## Attacker path

Call batchTransfer with crafted (count, value) pair that overflows count*value.

## Invariant broken

totalSupply equals sum of holder balances; transfers conserve supply.

## Victim impact

BEC token economically destroyed; trading halted on major exchanges.

**Loss check:** Token totalSupply no longer matches sum of legitimate holder balances.

## Root cause

Unchecked multiplication of (count * value) in pre-0.8 Solidity allowed wraparound when both factors were large.

**Protocol assumption that failed:** Assumed solidity arithmetic could not overflow at realistic input sizes.

## References

- [PeckShield analysis](https://blog.peckshield.com/2018/04/22/batchOverflow/)

## Missing verification steps

- [ ] Configure archival RPC for chain `ethereum` (alias `mainnet`).
- [ ] Run the command above against the declared fork block.
- [ ] Confirm all required assertion families pass.
- [ ] Replace the current-status section with run output.
- [ ] Update metadata `reproducibility` to `deterministic-confirmed` and `verification_status` to `verified` in the same change.

## Verifier

- **Handle:** _to be filled by verifier_
- **Date:** _to be filled by verifier_
- **Repository commit:** _to be filled by verifier_

---

Generated by `scripts/generate_verification_report.py` from `metadata/registry.json`. Content between the BEGIN/END GENERATED VERIFICATION SUMMARY markers is regenerated on every run. Manual sections (run transcripts, smoke-test evidence, archival verification logs) must live OUTSIDE those markers and will be preserved.

<!-- END GENERATED VERIFICATION SUMMARY -->

## Planned Assertion Hardening (Phase 6B)

- **Proposed assertions:**
  - Snapshot `attacker1` and `attacker2` BEC balances before `bec.batchTransfer(...)`.
  - After the call: `assertEq(a1After - a1Before, type(uint256).max/2 + 1, ...)` and the same for attacker2.
  - `assertGt(a1After + a2After, IERC20(address(bec)).totalSupply(), ...)` to fingerprint the supply-conservation break.
- **Proof target:** F1 (attacker profit) by exact-equality balance delta; F3 (invariant break) because the sum of two attacker balances exceeds `totalSupply`.
- **Risk:** additive-only. Three new state reads (`balanceOf` x4, `totalSupply` x1) and three assertions; no change to attack input or call path. Requires extending the local `BECToken` interface with `totalSupply()` or reusing `IERC20` from `src/interface.sol`.
- **Requires archival RPC:** yes — block 5483642 is archival-only on mainnet.
- **Patch class:** additive-only but requires archival verification.

## Phase 6D Assertion Patch

- **File modified:** `EVM/test/2018-04/Exploit_2018-04.t.sol`
- **Assertions added:**
  - `assertEq(a1After - a1Before, type(uint256).max / 2 + 1, "attacker1 mint delta")`
  - `assertEq(a2After - a2Before, type(uint256).max / 2 + 1, "attacker2 mint delta")`
  - `assertGt(a1After + a2After, bec.totalSupply(), "supply invariant broken")`
- **Interface change:** local `BECToken` interface extended with `totalSupply() external view returns (uint256)` — no other interface or call-site change.
- **Proof target:** F1 (attacker profit, exact equality on balance delta vs the supplied `_value`) and F3 (invariant break — combined attacker holdings exceed `totalSupply()`, the canonical `count*value` overflow fingerprint).
- **Exploit flow change:** none. Same receivers (`attacker1`, `attacker2`), same transfer amount (`type(uint256).max / 2 + 1`), same call order. Only `balanceOf` reads (x4) and one `totalSupply` read added.
- **Command run:** `cd EVM && forge test --match-path test/2018-04/Exploit_2018-04.t.sol -vvv`
- **Result:** test reverts in `setUp()` before any assertion executes. Public RPC at `mainnet` cannot serve historical state for block 5483642: `error code -32000: historical state ... is not available` for `0x1804c8AB1F12E6bbf3894d4083f33e07309d1f38`. This is an RPC archival limitation, not a PoC failure. The patched file compiles cleanly under `forge build` and passes `forge fmt --check`.
- **Whether assertions executed:** no — fork load failed, so no `testExploit` body executed at all.
- **Remaining verification limitation:** identical to pre-patch — archival mainnet RPC at block 5483642 is required to execute `setUp()` and reach the assertions. `verification_status` remains `not-run-no-rpc`; `reproducibility` remains `deterministic-likely-but-unverified`.
- **Metadata change:** `assertion_quality` raised from `none` to `strong` (exact-equality assertions on the literal exploit input + invariant-break inequality). No other metadata field changed.